import React from 'react';
import ReactDOM from 'react-dom';

const ProfileMain = () => (
  <section>
    <h1>Hello There</h1>
  </section>
)

ReactDOM.render(<ProfileMain/>, document.getElementById('profile-main'));
